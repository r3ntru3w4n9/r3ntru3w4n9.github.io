import 'package:flutter/material.dart';

/// Create a page for mobile browsers.
class MobilePage extends StatelessWidget {
  /// Build a mobile page.
  @override
  Widget build(BuildContext context) => throw UnimplementedError();
}
