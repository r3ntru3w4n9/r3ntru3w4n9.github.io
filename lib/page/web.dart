import 'package:flutter/material.dart';

/// Creates a page for normal browsers.
class WebPage extends StatelessWidget {
  /// Build a normal web page.
  @override
  Widget build(BuildContext context) => throw UnimplementedError();
}
